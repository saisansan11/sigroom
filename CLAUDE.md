# SIGROOM — ระบบจองและบริหารการใช้ห้อง โรงเรียนทหารสื่อสาร

ชื่อระบบ: **SIGROOM** (ใช้ในหัวเว็บ, Admin, เอกสาร) · ชื่อไทย: ระบบจองห้อง รร.ส.สส.

## บริบทที่ Claude ต้องรู้ก่อนทำงานทุกครั้ง
- ผู้พัฒนาทำงาน **คนเดียว ไม่ถนัดโค้ด** — อธิบายทุกขั้นเป็นภาษาไทยเข้าใจง่าย ให้คำสั่งที่รันได้ทีละคำสั่ง
  ไม่สันนิษฐานว่าผู้ใช้รู้ศัพท์เทคนิค และเมื่อทำอะไรเสร็จให้บอกวิธี "ดูผลด้วยตาตัวเอง" เสมอ
- ข้อกำหนดระบบ (SRS) อยู่ที่ `docs/room-booking-srs.html` — เป็น **เป้าหมายปลายทาง** ไม่ใช่ขอบเขต pilot
- ขอบเขตและลำดับงานจริงอยู่ที่ `docs/build-plan.md` — ทำทีละ milestone ห้ามข้าม
- Pilot ใช้ใน **เครือข่ายภายในเท่านั้น** ห้ามเปิดอินเทอร์เน็ตจนกว่าจะผ่านหมวด 10 ของ SRS

## Stack (ตัดสินใจแล้ว ห้ามเปลี่ยนโดยไม่ถาม)
- Python 3.12 + **Django 5.2 LTS** + **PostgreSQL 16** (ต้องมี extension `btree_gist`)
- จัดการแพ็กเกจด้วย `uv` (`uv sync`, `uv run manage.py ...`)
- หน้าเว็บ: Django templates + HTMX (ไม่ใช้ React/SPA) · ปฏิทิน: FullCalendar
- งานตามเวลา: management command `run_jobs` เรียกทุก 5 นาทีจาก Task Scheduler/cron
- ไม่ใช้ Docker ในเครื่องพัฒนา (ติดตั้ง PostgreSQL ตรง ๆ)

## กติกาที่ห้ามละเมิด
1. **การกันจองซ้อนต้องบังคับที่ฐานข้อมูล** ด้วย `ExclusionConstraint` บน `bookings.BookingResource`
   (SRS FR-09/35) ห้ามแทนที่ด้วยการตรวจในโค้ดอย่างเดียว
2. ทุกการเปลี่ยน schema ผ่าน `makemigrations` เท่านั้น และเป็นแบบ **expand/contract** (SRS NF-17)
3. กฎธุรกิจ (ตรวจชน, สถานะ, สิทธิ์) อยู่ใน `services.py` ของแต่ละแอป ไม่อยู่ใน view/template
4. ภาษาไทยทุกข้อความที่ผู้ใช้เห็น · วันที่แสดง พ.ศ. · เก็บ UTC (`USE_TZ=True`, `TIME_ZONE=Asia/Bangkok`)
5. ห้ามเก็บ secret ในโค้ด — ใช้ `.env` (มี `.env.example` เป็นแม่แบบ)
6. ทุก milestone ต้องมี test อย่างน้อยของกฎที่เพิ่ม และรัน `uv run pytest` ผ่านก่อนถือว่าเสร็จ
7. **ฟอร์มทุกช่อง "เลือกจากรายการที่ใช้บ่อย + พิมพ์เองได้"** (ใช้ `<input list>`/datalist ไม่ต้องมี JS)
   ยกเว้นฟิลด์อ้างอิงของระบบ (ห้อง, อุปกรณ์ส่วนกลาง, หน่วยงาน, ประเภทการใช้งาน, สถานะ, ระดับการมองเห็น)
   ที่ต้อง "เลือกอย่างเดียว" แต่ให้พิมพ์ค้นหาในรายการได้ — เพราะรายงานและกฎรายห้องต้องอ้างค่าที่แน่นอน
8. ลำดับงานแต่ละ milestone: **ร่างหน้าจอเป็นข้อความให้ผู้ใช้ดูก่อน → เขียนโค้ดด้วย UI เรียบแต่สม่ำเสมอ → ขัด UX/UI รวบยอดใน M6**
   ห้ามใช้เวลากับความสวยก่อนที่ขั้นตอนจะถูกต้อง

## โครงสร้าง
```
config/      settings, urls
accounts/    ผู้ใช้ หน่วยงาน บทบาท
resources/   ทะเบียนห้อง/อุปกรณ์ กฎรายห้อง ผู้อนุมัติ
bookings/    การจอง ช่วงถือครอง ชุดการจอง amendment
approvals/   อนุมัติ/ปฏิเสธ SLA ผู้รักษาการ (M3)
docs/        SRS, build plan, ADR
```

## คำสั่งประจำ
```
uv sync                         # ติดตั้งแพ็กเกจ
uv run manage.py migrate        # สร้าง/อัปเดตตาราง
uv run manage.py runserver      # เปิดเว็บที่ http://127.0.0.1:8000
uv run manage.py createsuperuser
uv run pytest                   # ทดสอบ
```

## การรายงานกลับผู้ใช้
- ทุกครั้งที่แก้ไฟล์ ให้สรุปว่า "แก้อะไร ทำไม และจะเห็นผลที่ไหน"
- ถ้าต้องติดตั้งโปรแกรมหรือแก้ระบบปฏิบัติการ ให้ผู้ใช้รันเอง พร้อมอธิบายว่ามันทำอะไร
